import 'package:flutter/material.dart';
import 'widgetsProfile/headerProfile.dart';

class ProfileTrips extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Stack(
      children: [
        ListView(
          children: [],
        ),
        HeaderProfile(),
      ],
    );
  }
}
